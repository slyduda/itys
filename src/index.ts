import { addStateMachine } from "./machine";
import type {
  MachineDict,
  TransitionResult,
  TransitionOptions,
  StateMachineOptions,
} from "./types";

export {
  addStateMachine,
  MachineDict,
  TransitionOptions,
  TransitionResult,
  StateMachineOptions,
};
